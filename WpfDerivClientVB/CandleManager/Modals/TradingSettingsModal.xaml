<Window x:Class="WpfDerivClientVB.TradingSettingsModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuración de Sesión" Height="500" Width="440"
        Background="#0E0E1A" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" WindowStyle="ToolWindow">

    <Window.Resources>
        <!-- ── Tipografía global ── -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <!-- ── TextBox oscuro ── -->
        <Style TargetType="TextBox">
            <Setter Property="Background"   Value="#313244"/>
            <Setter Property="Foreground"   Value="#CDD6F4"/>
            <Setter Property="BorderBrush"  Value="#45475A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"      Value="6,4"/>
            <Setter Property="FontFamily"   Value="Segoe UI"/>
            <Setter Property="FontSize"     Value="12"/>
        </Style>

        <!-- ── Labels ── -->
        <Style TargetType="Label">
            <Setter Property="Foreground"  Value="#A6ADC8"/>
            <Setter Property="FontFamily"  Value="Segoe UI"/>
            <Setter Property="FontWeight"  Value="SemiBold"/>
            <Setter Property="FontSize"    Value="11"/>
            <Setter Property="Padding"     Value="0,0,0,3"/>
        </Style>

        <!-- ── ComboBoxItem con template oscuro ── -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="Padding"    Value="8,5"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize"   Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="brd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="brd" Property="Background" Value="#45475A"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="brd" Property="Background" Value="#1A3A5A"/>
                                <Setter Property="Foreground" Value="#89B4FA"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#45475A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── ComboBox con template completo (popup oscuro) ── -->
        <Style TargetType="ComboBox">
            <Setter Property="Background"       Value="#313244"/>
            <Setter Property="Foreground"       Value="#CDD6F4"/>
            <Setter Property="BorderBrush"      Value="#45475A"/>
            <Setter Property="BorderThickness"  Value="1"/>
            <Setter Property="Padding"          Value="8,5"/>
            <Setter Property="Height"           Value="30"/>
            <Setter Property="FontFamily"       Value="Segoe UI"/>
            <Setter Property="FontSize"         Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="toggleBtn" Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                              RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="tBrd"
                                                Background="#313244"
                                                BorderBrush="#45475A"
                                                BorderThickness="1"
                                                CornerRadius="3">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="22"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0"/>
                                                <Border Grid.Column="1" Background="#45475A" CornerRadius="0,3,3,0">
                                                    <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="#CDD6F4"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="tBrd" Property="BorderBrush" Value="#89B4FA"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="tBrd" Property="BorderBrush" Value="#89B4FA"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="tBrd" Property="Opacity" Value="0.4"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="contentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="8,0,26,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              TextBlock.Foreground="#CDD6F4"/>

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border x:Name="dropBrd"
                                        Background="#313244"
                                        BorderBrush="#89B4FA"
                                        BorderThickness="1"
                                        CornerRadius="0,0,4,4"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Separador ── -->
        <Style TargetType="Separator">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Margin"     Value="0,12,0,12"/>
        </Style>

        <!-- ── Botones base ── -->
        <Style TargetType="Button">
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Padding"         Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="brd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="brd" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="brd" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#89B4FA"/>
            <Setter Property="Foreground" Value="#11111B"/>
        </Style>
        <Style x:Key="SecondaryBtn" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- Header -->
            <RowDefinition Height="*"/>      <!-- Form -->
            <RowDefinition Height="Auto"/>   <!-- Footer buttons -->
        </Grid.RowDefinitions>

        <!-- ══ HEADER ══════════════════════════════════════════════ -->
        <Border Grid.Row="0"
                Background="#12122A"
                BorderBrush="#313244" BorderThickness="0,0,0,1"
                Padding="20,16">
            <StackPanel>
                <TextBlock Text="⚙  Configuración de Sesión"
                           FontSize="16" FontWeight="Bold"
                           Foreground="#89B4FA"/>
                <TextBlock Text="Selecciona la plataforma, cliente, cuenta y activo con que operará este visor."
                           FontSize="11" Foreground="#6C7086"
                           TextWrapping="Wrap" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- ══ FORM ═════════════════════════════════════════════════ -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20,16,20,8">

                <!-- Plataforma -->
                <Label Content="PLATAFORMA"/>
                <ComboBox x:Name="cmbPlataforma"
                          SelectionChanged="CmbPlataforma_SelectionChanged"
                          Margin="0,0,0,14">
                    <ComboBoxItem Content="Deriv"   Tag="deriv"/>
                </ComboBox>

                <Separator/>

                <!-- Cliente -->
                <Label Content="CLIENTE"/>
                <Grid Margin="0,0,0,14">
                    <ComboBox x:Name="cmbCliente"
                              DisplayMemberPath="Name"
                              IsEnabled="False"
                              SelectionChanged="CmbCliente_SelectionChanged"/>
                    <TextBlock x:Name="txtLoadingClientes"
                               Text="Cargando..."
                               Foreground="#6C7086" FontSize="11"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"
                               Margin="0,0,30,0"
                               Visibility="Collapsed"/>
                </Grid>

                <!-- Tipo de Cuenta -->
                <Label Content="TIPO DE CUENTA"/>
                <ComboBox x:Name="cmbTipoCuenta"
                          IsEnabled="False"
                          SelectionChanged="CmbTipoCuenta_SelectionChanged"
                          Margin="0,0,0,14">
                    <ComboBoxItem Content="Demo  (Virtual)"  Tag="demo"/>
                    <ComboBoxItem Content="Real"              Tag="real"/>
                </ComboBox>

                <!-- Badge de credenciales activas -->
                <Border x:Name="pnlCredBadge"
                        Background="#1E1E2E"
                        BorderBrush="#45475A" BorderThickness="1"
                        CornerRadius="4" Padding="10,6"
                        Margin="0,0,0,14"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock x:Name="txtCredToken"
                                   Text="Token: —"
                                   FontSize="11" Foreground="#A6ADC8"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock x:Name="txtCredAccount"
                                   Text="Account: —"
                                   FontSize="11" Foreground="#A6ADC8"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>

                <Separator/>

                <!-- Activo -->
                <Label Content="ACTIVO (SÍMBOLO)"/>
                <Grid Margin="0,0,0,4">
                    <ComboBox x:Name="cmbActivo"
                              IsEnabled="False"
                              DisplayMemberPath="Display"/>
                    <TextBlock x:Name="txtLoadingActivos"
                               Text="Cargando desde Deriv..."
                               Foreground="#6C7086" FontSize="11"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"
                               Margin="0,0,30,0"
                               Visibility="Collapsed"/>
                </Grid>
                <TextBlock x:Name="txtActivosError"
                           Foreground="#F38BA8" FontSize="10"
                           Margin="0,0,0,4" Visibility="Collapsed"
                           TextWrapping="Wrap"/>

            </StackPanel>
        </ScrollViewer>

        <!-- ══ FOOTER ════════════════════════════════════════════════ -->
        <Border Grid.Row="2"
                Background="#12122A"
                BorderBrush="#313244" BorderThickness="0,1,0,0"
                Padding="20,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnCancelar"
                        Content="Cancelar"
                        Style="{StaticResource SecondaryBtn}"
                        Width="100" Margin="0,0,8,0"
                        Click="BtnCancelar_Click"/>
                <Button x:Name="btnGuardar"
                        Content="✔  Guardar"
                        Style="{StaticResource PrimaryBtn}"
                        Width="120" IsEnabled="False"
                        Click="BtnGuardar_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
